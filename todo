- VPC view
- Bug: Support for "${resource1.x}-${resource2.y}-${resource3.z}" like format
- Details
  - Show side bar with details (all the configs)
- Edge generation logic
  - Key lookup outside modules, link to the module block instead of creating a new node
- TF vars window and workspaces
- Collapsible nodes
  - Meta edges
  - https://github.com/iVis-at-Bilkent/cytoscape.js-expand-collapse
- Clicking on a node, highlights all connected nodes
